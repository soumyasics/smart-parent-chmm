// import { useSelector } from "react-redux"
// import { RootState } from "../../../redux/store";
export const ParentProtect = () => {
    // const {isAuthenticated, usertype} = useSelector((state: RootState) => state.user);
    return (
        <div>
            {/* {isAu } */}
            <h1>todo: Parent protect </h1>
        </div>
    )
}